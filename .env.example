# This is an example file. Create a .env.local file in the root directory
# and add your actual database connection string.
# DO NOT commit your .env.local file to version control.

DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"
